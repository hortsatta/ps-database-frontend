const fonts = {
  heading: 'Clonoid, sans-serif',
  body: 'HelNeue, sans-serif'
};

const styles = {
  global: {
    body: {
      bg: '#000000',
      color: '#ffffff'
    }
  }
};

const theme = {
  fonts,
  styles
};

export { fonts, theme }
