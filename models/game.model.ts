import { Review } from './review.model';

export type Game = {
  slug: string;
  title: string;
  description: string;
  // developer: string;
  // publisher: string;
  // releaseDate: Date;
  // coverImage: string;
  // featureImage: string;
  // platforms: string[];
  // userId: number;
  // reviews: Review[];
}
